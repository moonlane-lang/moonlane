# Blocked

Tasks waiting on external blockers or dependencies.

These tasks have clear blockers (listed in the task file). Move back to `open/` or `in-progress/` when blockers are resolved. Update the task with reason and expected resolution date.
