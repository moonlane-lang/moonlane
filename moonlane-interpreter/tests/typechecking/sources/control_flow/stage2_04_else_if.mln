// Stage 2: else-if chains (statement form)
//
// The else branch of an if_expr can be another if_expr (chaining).
// Each condition must be Bool; the overall type is Unit when used as a statement.

fun check_range(x: Int) {
    if (x < 0) {
        return;
    } else if (x > 100) {
        return;
    } else {
        return;
    }
}

fun multi_branch(a: Bool, b: Bool) {
    if (a) {
    } else if (b) {
    }
}
