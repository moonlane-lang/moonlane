// Stage 3 negative: if without else where then-branch is non-Unit
//
// An if without an else has type Unit; the then-branch must also produce Unit.
// Using a non-Unit value (like Int) in the then-branch with no else is a type error.

let x: Int = 5;
if (x > 0) { x }; // ERROR[T0001]
